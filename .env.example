# ========================================
# LISTED - Environment Variables Template
# ========================================
# Copy this file to .env and fill in the values
# For Railway deployment, set these in the Railway dashboard

# ========================================
# FLASK BACKEND
# ========================================

# Flask environment (development, production)
FLASK_ENV=production

# Flask secret key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-secret-key-here-generate-a-secure-one

# Flask app module
FLASK_APP=wsgi:app

# Disable Flask debug mode in production
FLASK_DEBUG=0

# ========================================
# DATABASE (MySQL)
# ========================================

# Railway will provide this automatically via their MySQL plugin
# Format: mysql://user:password@host:port/database
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/newlisted_db

# Alternative: Individual components (if not using DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=user
# DB_PASSWORD=password
# DB_NAME=newlisted_db

# ========================================
# FRONTEND (React + Vite)
# ========================================

# Backend API URL for production
# In Railway, this would be: https://your-backend-service.up.railway.app
VITE_API_URL=http://localhost:5000

# ========================================
# PRODUCTION SETTINGS
# ========================================

# Port (Railway assigns this dynamically via $PORT)
PORT=5000

# Gunicorn workers (adjust based on Railway plan)
WEB_CONCURRENCY=4

# ========================================
# CORS (Optional - if frontend and backend on different domains)
# ========================================

# Allowed origins for CORS
# CORS_ORIGINS=https://your-frontend.up.railway.app

# ========================================
# NOTES FOR RAILWAY DEPLOYMENT
# ========================================
#
# 1. Do NOT commit .env to Git (it's in .gitignore)
# 2. Set these variables in Railway dashboard under "Variables"
# 3. Railway MySQL plugin will auto-set DATABASE_URL
# 4. Railway will auto-set PORT for each service
# 5. Generate a strong SECRET_KEY for production
#
