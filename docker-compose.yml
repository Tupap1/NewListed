version: '3.8'

services:
  ccnsoft-core:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Optional: persist the sqlite db if needed
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/test.db
